/**
 * BottomSpacer - Provides safe spacing at the bottom of scrollable content
 * to prevent content from being hidden behind the bottom navigation bar
 */
export function BottomSpacer() {
  return <div className="h-20" />;
}
